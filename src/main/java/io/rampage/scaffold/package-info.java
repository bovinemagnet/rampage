/**
 * Importers that scaffold Rampage scenario YAML from external API descriptions.
 *
 * <p>{@code HarImporter} converts recorded HTTP Archive (HAR) captures and
 * {@code OpenApiImporter} converts OpenAPI specifications into starter scenario
 * files, giving new projects a working configuration to refine rather than a blank
 * page.</p>
 */
package io.rampage.scaffold;
