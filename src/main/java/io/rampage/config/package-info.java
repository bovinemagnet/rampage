/**
 * Configuration entry points for the Rampage load-testing framework.
 *
 * <p>Holds {@code ConfigValidatorMain}, the standalone command-line entry point that
 * loads and validates the environment, run, and scenario YAML files without executing
 * a load test. The configuration models themselves live in
 * {@code io.rampage.config.model}.</p>
 */
package io.rampage.config;
